---
title: "Vulkan Image Barrier 图像屏障"
description: "Vulkan 同步模型里 image layout transition 与 barrier 的作用范围。"
date: 2026-08-08
tags: ["vulkan", "graphics"]
draft: false
---

# Vulkan Image Barrier 图像屏障

Vulkan 要求显式同步资源访问,Image Barrier 用于处理 layout transition 与内存可见性。

## 典型场景

从渲染目标过渡到可采样图像:

```cpp
VkImageMemoryBarrier barrier{};
barrier.sType = VK_STRUCTURE_TYPE_IMAGE_MEMORY_BARRIER;
barrier.oldLayout = VK_IMAGE_LAYOUT_COLOR_ATTACHMENT_OPTIMAL;
barrier.newLayout = VK_IMAGE_LAYOUT_SHADER_READ_ONLY_OPTIMAL;
barrier.srcAccessMask = VK_ACCESS_COLOR_ATTACHMENT_WRITE_BIT;
barrier.dstAccessMask = VK_ACCESS_SHADER_READ_BIT;
```

## 三个组成部分

Barrier 由三个部分组成:

1. `srcStageMask` — 之前的执行阶段
2. `dstStageMask` — 之后的执行阶段
3. dependency flags

```cpp
vkCmdPipelineBarrier(
    cmd, srcStage, dstStage,
    0, 0, nullptr, 0, nullptr, 1, &barrier);
```

> Layout transition 只会在 barrier 中执行,不会自动发生。
