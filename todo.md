# TODO

Bézier 曲线, B-Rep, NURBS

```js
for (let alpha = 0.0; alpha < 1.0; alpha += step) {
  for (let beta = 0.0; beta < 1.0 - alpha; beta += step) {
    let gamma = 1 - alpha - beta;
  }
}
```

类型系统, covariant, contravariant, invariant, 对比 C++ 和 C#

名义子类型, 结构子类型, subtype polymorphism, ad-hoc polymorphism, parametric polymorphism

类型构造器

lambda 演算, Church

Rust 生命周期与子类型

C++ trait 萃取 类型构造器, 特化白名单

template template parameter
